<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.project.stardictionary">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".startActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".visitPage" />
        <activity android:name=".Videos" />
        <activity android:name=".buttonActivity" />
        <activity android:name=".articlesActivity" />
        <activity android:name=".content_writer" />
        <activity android:name=".content_writer2" />
        <activity android:name=".signUpActivity" />
        <activity android:name=".ListDataActivity"></activity>
    </application>

</manifest>